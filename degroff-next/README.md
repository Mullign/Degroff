# degroff-next

Next.js application for the PitotShield V2™ SmartCover™ marketing site.

**Full documentation** (setup, environment variables, deployment, architecture) is in the [**repository root README**](../README.md).

## Quick start

```bash
npm install
# Create .env.local — see root README for NEXT_PUBLIC_* variables
npm run dev
```

From this directory: `npm run build` produces a static export in `out/` (see root README for `NEXT_PUBLIC_*` variables).
